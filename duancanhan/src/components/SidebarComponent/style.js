import styled from 'styled-components';

export const WrapperSidebar = styled.div`
    width: 250px;
    flex-shrink: 0;
    position: sticky;
    top: 130px; /* Adjust based on navbar + utilities bar height */
    overflow: visible;
`;
